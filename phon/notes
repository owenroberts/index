import codecs
f = codecs.open('unicode.rst', encoding='utf-8')
for line in f:
    print repr(line)

f = open('list.txt', 'w')
f.close()

import epitran
epi = epitran.Epitran('eng-Latn')
spa-Latn

print(epi.transliterate(u'Düğün'))




10.4.2017
- what texts do we want to work with?
- what to do with them to combine?
- discrete index of ipa parts in text?
- 